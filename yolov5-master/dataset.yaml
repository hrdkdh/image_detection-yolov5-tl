# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: E:/pgu_safety_dataset  # dataset root dir
train: training_dataset  # train images (relative to 'path') 128 images
val: validation_dataset  # val images (relative to 'path') 128 images
test:  # test images (optional)

# Classes
nc: 2  # number of classes
names: ['no helmet', 'helmet']  # class names